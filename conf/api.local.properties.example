# 本地开发覆盖配置模板
# 将此文件复制为 conf/api.local.properties（已加入 .gitignore，不会提交），
# 并填入真实值。该文件中的值会覆盖 conf/api.properties 中的对应键。

# MySQL 密码
mysql.password=<your-mysql-password>

# Nexus 制品库密码
nexus.password=<your-nexus-password>

# RustFS / MinIO 密钥
rustfs.secret_key=<your-rustfs-secret-key>
