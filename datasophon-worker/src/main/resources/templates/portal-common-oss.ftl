<#noparse>
oss:
  minio:
    enabled: true
    endpoint: ${oss.endpoint}
    access-key: ${oss.access-key}
    secret-key: ${oss.secret-key}
    prefix: ${oss.prefix}
    public-bucket: ${oss.public-bucket}
</#noparse>