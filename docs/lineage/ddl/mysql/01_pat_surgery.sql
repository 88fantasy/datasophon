-- 病人手术表（源，全 VARCHAR 镜像，D10）
-- 列清单来源：docs/lineage/原始表结构示例数据（MySQL数据库的4张表+绩效4张表）.xlsx
-- D10：全列 1:1 镜像 + 全 VARCHAR。列宽分档（utf8mb4 撞过 InnoDB 65535 字节行上限后收紧，2026-08-06 实测）：
-- 默认 64 字符（UUID/代码/时间戳字符串）；长文本关键字 500；JSON 小字段 300；JSON 大字段 1000
-- 本表 JSON 列：ANES_ASSISTANT, ANES_EXTEND_INFO, OR_EXTEND_INFO, QC_EXTEND_INFO, SURGERY_ASSISTANT, SURGERY_DOCTOR

DROP TABLE IF EXISTS `pat_surgery`;
CREATE TABLE `pat_surgery` (
  `ID` VARCHAR(64) NOT NULL COMMENT '主键（业务侧生成，非自增）',
  `PATIENT_ID` VARCHAR(64) NULL,
  `PATIENT_SYNC_ID` VARCHAR(64) NULL,
  `RECORD_ID` VARCHAR(64) NULL,
  `RECORD_TYPE` VARCHAR(64) NULL,
  `TYPE` VARCHAR(64) NULL,
  `GOING_STATUS` VARCHAR(64) NULL,
  `INFECTION_COUNT` VARCHAR(64) NULL,
  `ABNORMAL_COUNT` VARCHAR(64) NULL,
  `VERIFY_CONTENT` VARCHAR(500) NULL,
  `NAME` VARCHAR(64) NULL,
  `ID_CARD` VARCHAR(64) NULL,
  `GENDER` VARCHAR(64) NULL,
  `AGE` VARCHAR(64) NULL,
  `HEIGHT` VARCHAR(64) NULL,
  `WEIGHT` VARCHAR(64) NULL,
  `BLOOD_TYPE` VARCHAR(64) NULL,
  `CONTACTS_PHONE` VARCHAR(64) NULL,
  `ADDRESS` VARCHAR(500) NULL,
  `ADMISSION_NUMBER` VARCHAR(64) NULL,
  `ADMISSION_TIMES` VARCHAR(64) NULL,
  `BED_NUMBER` VARCHAR(64) NULL,
  `HOSPITAL_ID` VARCHAR(64) NULL,
  `HOSPITAL_NAME` VARCHAR(64) NULL,
  `DEPT_ID` VARCHAR(64) NULL,
  `DEPT_NAME` VARCHAR(64) NULL,
  `DOCTOR_DEPT_ID` VARCHAR(64) NULL,
  `DOCTOR_DEPT_NAME` VARCHAR(64) NULL,
  `APPLY_DEPT_ID` VARCHAR(64) NULL,
  `APPLY_DEPT_NAME` VARCHAR(64) NULL,
  `DIAGNOSIS_CONTENT` VARCHAR(500) NULL,
  `INFECTION_HISTORY` VARCHAR(500) NULL,
  `SEPICAL_PREPARATION` VARCHAR(500) NULL,
  `ROOM_ID` VARCHAR(64) NULL,
  `ROOM_NAME` VARCHAR(64) NULL,
  `IS_TRANSFER_ROOM` VARCHAR(64) NULL,
  `SERIAL_INTERVAL` VARCHAR(64) NULL,
  `SURGERY_SERIAL` VARCHAR(64) NULL,
  `SURGERY_DATE` VARCHAR(64) NULL,
  `SURGERY_NAME` VARCHAR(500) NULL,
  `SURGERY_LEVEL` VARCHAR(64) NULL,
  `SURGERY_INCISION` VARCHAR(64) NULL,
  `SURGERY_DOCTOR_ID` VARCHAR(64) NULL,
  `SURGERY_DOCTOR_NAME` VARCHAR(64) NULL,
  `SURGERY_DOCTOR` VARCHAR(300) NULL,
  `SURGERY_ASSISTANT` VARCHAR(300) NULL,
  `CIRCUIT_NURSE1_ID` VARCHAR(64) NULL,
  `CIRCUIT_NURSE1_NAME` VARCHAR(64) NULL,
  `CIRCUIT_NURSE2_ID` VARCHAR(64) NULL,
  `CIRCUIT_NURSE2_NAME` VARCHAR(64) NULL,
  `EQUIPMENT_NURSE1_ID` VARCHAR(64) NULL,
  `EQUIPMENT_NURSE1_NAME` VARCHAR(64) NULL,
  `EQUIPMENT_NURSE2_ID` VARCHAR(64) NULL,
  `EQUIPMENT_NURSE2_NAME` VARCHAR(64) NULL,
  `PERFUSION_NURSE_ID` VARCHAR(64) NULL,
  `PERFUSION_NURSE_NAME` VARCHAR(64) NULL,
  `APPLY_TIME` VARCHAR(64) NULL,
  `OR_DEPT_ID` VARCHAR(64) NULL,
  `OR_DEPT_NAME` VARCHAR(64) NULL,
  `EC_DEPT_ID` VARCHAR(64) NULL,
  `EC_DEPT_NAME` VARCHAR(64) NULL,
  `AN_DEPT_ID` VARCHAR(64) NULL,
  `AN_DEPT_NAME` VARCHAR(64) NULL,
  `IS_SECOND` VARCHAR(64) NULL,
  `SECOND_REASON` VARCHAR(500) NULL,
  `ANES_DOCTOR1_ID` VARCHAR(64) NULL,
  `ANES_DOCTOR1_NAME` VARCHAR(64) NULL,
  `ANES_DOCTOR2_ID` VARCHAR(64) NULL,
  `ANES_DOCTOR2_NAME` VARCHAR(64) NULL,
  `ANES_ASSISTANT` VARCHAR(300) NULL,
  `PERFUSION_DOCTOR_ID` VARCHAR(64) NULL,
  `PERFUSION_DOCTOR_NAME` VARCHAR(64) NULL,
  `ANES_TYPE_CODE` VARCHAR(64) NULL,
  `ANES_TYPE_NAME` VARCHAR(64) NULL,
  `ANES_TYPE_PARENT_CODE` VARCHAR(64) NULL,
  `ANES_TYPE_PARENT_NAME` VARCHAR(64) NULL,
  `IS_ANALGESIC` VARCHAR(64) NULL,
  `IS_FIRST` VARCHAR(64) NULL,
  `STATE` VARCHAR(64) NULL,
  `PREVIOUS_LEAVE_TIME` VARCHAR(64) NULL,
  `INDUCE_BEGIN_TIME` VARCHAR(64) NULL,
  `INDUCE_END_TIME` VARCHAR(64) NULL,
  `SURGERY_ENTRY_TIME` VARCHAR(64) NULL,
  `SURGERY_LEAVE_TIME` VARCHAR(64) NULL,
  `SURGERY_BEGIN_TIME` VARCHAR(64) NULL,
  `SURGERY_END_TIME` VARCHAR(64) NULL,
  `ANES_BEGIN_TIME` VARCHAR(64) NULL,
  `ANES_END_TIME` VARCHAR(64) NULL,
  `CREATOR_ID` VARCHAR(64) NULL,
  `CREAT_TIME` VARCHAR(64) NULL,
  `MODIFIER_ID` VARCHAR(64) NULL,
  `MODIFY_TIME` VARCHAR(64) NULL,
  `ASA_CODE` VARCHAR(64) NULL,
  `LEAVE_WARD_TIME` VARCHAR(64) NULL,
  `ARRIVAL_OR_DEPT_TIME` VARCHAR(64) NULL,
  `INTO_OR_DEPT_TIME` VARCHAR(64) NULL,
  `DOCTOR_ARRIVAL_TIME` VARCHAR(64) NULL,
  `PACU_DOCTOR_ID` VARCHAR(64) NULL,
  `PACU_DOCTOR_ID1` VARCHAR(64) NULL,
  `PACU_ENTER_NURSE_ID` VARCHAR(64) NULL,
  `PACU_LEAVE_NURSE_ID` VARCHAR(64) NULL,
  `PACU_BEGIN_TIME` VARCHAR(64) NULL,
  `PACU_END_TIME` VARCHAR(64) NULL,
  `PACU_REVIVAL_TIME` VARCHAR(64) NULL,
  `DISCHARGE_DATE` VARCHAR(64) NULL,
  `OR_EXTEND_INFO` TEXT NULL,
  `ANES_EXTEND_INFO` TEXT NULL,
  `QC_EXTEND_INFO` TEXT NULL,
  `LEAVE_ALDRETE` VARCHAR(64) NULL,
  `SEAL_MEDICAL` VARCHAR(64) NULL,
  `VALID` VARCHAR(64) NULL,
  PRIMARY KEY (`ID`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='病人手术表（源，全 VARCHAR 镜像，D10）';
